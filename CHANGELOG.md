# v0.0.132
- Implemented 'Exclude' functionality in the Viewer.


# v0.0.131
- Fixed bug in closing Viewer Windows.
- Implemented a new TrackMate Single mode


# v0.0.131
- More permissive handling of boolean values in CSV files.
- Correct handling of switching between projects 
- Correct handling of Trackmate processing interruption
- Introduced minTracksToCalculate. This allows skipping the calculation of squares with less than a certain number of tracks.


# v0.0.130
- (Development) Reorganisation of PaintConstants
- (Development) Harmonisation of coding style (elimination of unnecessary ChatGPT vartiability).
- Made the movie shower more robust (data can always be read from external disk)


# v0.0.129
- (Development) Significant refactoring of packages
- (Development) Significant replacement of hard-coded strings with constants (involving most classes)
- (Development) Several large classes have been refactored into small ones for improved maintainability.
- Implemented the Viewer option to import overrides of earlier sessions (checkbox left bottom).


# v0.0.127
- TrackMte plugin supports a parameter sweep on Threshold (handy for the initial determination for optimal threshold values for a newexperiment).
- Misformed JSON files are recognised and handled gracefully. 
- (Development) Several large classes have been refactored into small ones for improved maintainability.

# v0.0.122
- New installer for Windows and macOS allows installation of individual components. The location of the  Fiji installation, if not found, can be specified by the user (and the choice is remembered for later installations).
- The Viewer has a new 'Show Squares' option to show the data of all squares.

# Initial release notes
