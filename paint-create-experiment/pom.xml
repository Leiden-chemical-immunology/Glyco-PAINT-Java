<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- =============================================================== -->
    <!-- Project hierarchy -->
    <!-- =============================================================== -->
    <parent>
        <groupId>com.github.jjabakker</groupId>
        <artifactId>paint-parent</artifactId>
        <version>0.0.126-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>paint-create-experiment</artifactId>
    <packaging>jar</packaging>

    <name>Paint Create Experiment</name>
    <description>Standalone desktop app for creating experiments</description>

    <!-- =============================================================== -->
    <!-- Application properties -->
    <!-- =============================================================== -->
    <properties>
        <project.build.outputTimestamp>2025-11-10T08:10:14Z</project.build.outputTimestamp>
        <app.name>Create Experiment</app.name>
        <app.bundle>${project.build.directory}/${app.name}.app</app.bundle>
        <app.icon.macos>${project.basedir}/src/main/resources/icons/paint-create-experiment.icns</app.icon.macos>
        <app.icon.windows>${project.basedir}/src/main/resources/icons/paint-create-experiment.ico</app.icon.windows>
    </properties>

    <!-- =============================================================== -->
    <!-- Dependencies -->
    <!-- =============================================================== -->
    <dependencies>
        <dependency>
            <groupId>com.github.jjabakker</groupId>
            <artifactId>paint-shared-utils</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-csv</artifactId>
            <version>1.9.0</version>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-nop</artifactId>
            <version>1.7.36</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <!-- =============================================================== -->
    <!-- Build configuration -->
    <!-- =============================================================== -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.7.1</version>
                <configuration>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                    <archive>
                        <manifest>
                            <mainClass>createexperiment.CreateExperimentUI</mainClass>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix></classpathPrefix>
                        </manifest>
                        <manifestEntries>
                            <Built-By>Glyco-PAINT Build System</Built-By>
                            <Implementation-Title>${project.artifactId}</Implementation-Title>
                            <Implementation-Version>${project.version}</Implementation-Version>
                        </manifestEntries>
                    </archive>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <id>make-fat-jar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- =============================================================== -->
    <!-- OS-specific build profiles -->
    <!-- =============================================================== -->
    <profiles>

        <!-- macOS .app bundle profile -->
        <profile>
            <id>macos-appbundle</id>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>create-app-bundle</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <echo message="Creating macOS app bundle at ${app.bundle}"/>
                                        <delete dir="${app.bundle}" failonerror="false"/>
                                        <delete file="${project.build.directory}/${app.name}.app.zip" failonerror="false"/>

                                        <!-- Create bundle structure -->
                                        <mkdir dir="${app.bundle}/Contents/MacOS"/>
                                        <mkdir dir="${app.bundle}/Contents/Java"/>
                                        <mkdir dir="${app.bundle}/Contents/Resources"/>

                                        <!-- Copy binaries and resources -->
                                        <copy file="${project.build.directory}/${project.artifactId}-${project.version}.jar"
                                              todir="${app.bundle}/Contents/Java" failonerror="false"/>
                                        <copy file="${app.icon.macos}" todir="${app.bundle}/Contents/Resources" failonerror="false"/>

                                        <!-- Info.plist -->
                                        <echo file="${app.bundle}/Contents/Info.plist"><![CDATA[
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key><string>${app.name}</string>
    <key>CFBundleDisplayName</key><string>${app.name}</string>
    <key>CFBundleExecutable</key><string>launch</string>
    <key>CFBundleIconFile</key><string>${project.artifactId}.icns</string>
    <key>CFBundleIdentifier</key><string>com.github.jjabakker.${app.name}</string>
    <key>CFBundleVersion</key><string>${project.version}</string>
    <key>CFBundlePackageType</key><string>APPL</string>
    <key>NSHighResolutionCapable</key><true/>
    <key>JVMVersion</key><string>1.8*</string>
</dict>
</plist>
]]></echo>

                                        <!-- Launch script -->
                                        <echo file="${app.bundle}/Contents/MacOS/launch"><![CDATA[
#!/bin/bash
# macOS launcher for ${app.name}
cd "$(dirname "$0")/../Java"

# Use JAVA_HOME if set; otherwise auto-detect Java 8 via /usr/libexec/java_home
if [ -z "$JAVA_HOME" ]; then
  JAVA_CMD="$(/usr/libexec/java_home -v1.8 2>/dev/null)/bin/java"
  if [ ! -x "$JAVA_CMD" ]; then
    echo "❌ Java 8 not found. Please install Java 8 or set JAVA_HOME."
    exit 1
  fi
else
  JAVA_CMD="$JAVA_HOME/bin/java"
fi

exec "$JAVA_CMD" -Xmx2G -jar "${project.artifactId}-${project.version}.jar"
]]></echo>
                                        <fixcrlf eol="unix" file="${app.bundle}/Contents/MacOS/launch"/>
                                        <chmod file="${app.bundle}/Contents/MacOS/launch" perm="755"/>

                                        <!-- Install to Glyco-PAINT folder -->
                                        <mkdir dir="${user.home}/Applications/Glyco-PAINT"/>
                                        <delete dir="${user.home}/Applications/Glyco-PAINT/${app.name}.app" quiet="true"/>
                                        <copy todir="${user.home}/Applications/Glyco-PAINT" failonerror="false" overwrite="true">
                                            <fileset dir="${project.build.directory}">
                                                <include name="${app.name}.app/**"/>
                                            </fileset>
                                        </copy>
                                        <exec executable="chmod" failonerror="false">
                                            <arg value="755"/>
                                            <arg value="${user.home}/Applications/Glyco-PAINT/${app.name}.app/Contents/MacOS/launch"/>
                                        </exec>
                                        <echo message="✅ Installed to ~/Applications/Glyco-PAINT/${app.name}.app"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Windows .exe launcher profile -->
        <profile>
            <id>windows-exe</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.akathist.maven.plugins.launch4j</groupId>
                        <artifactId>launch4j-maven-plugin</artifactId>
                        <version>2.5.0</version>
                        <executions>
                            <execution>
                                <id>l4j-create-experiment</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>launch4j</goal>
                                </goals>
                                <configuration>
                                    <outfile>${project.build.directory}/${app.name}.exe</outfile>
                                    <jar>${project.build.directory}/${project.artifactId}-${project.version}.jar</jar>
                                    <dontWrapJar>false</dontWrapJar>
                                    <icon>${app.icon.windows}</icon>
                                    <jre>
                                        <minVersion>1.8.0</minVersion>
                                    </jre>
                                    <classPath>
                                        <mainClass>createexperiment.CreateExperimentUI</mainClass>
                                    </classPath>
                                    <headerType>gui</headerType>
                                    <errTitle>${app.name}</errTitle>
                                    <versionInfo>
                                        <fileVersion>1.0.0.0</fileVersion>
                                        <txtFileVersion>${project.version}</txtFileVersion>
                                        <productVersion>1.0.0.0</productVersion>
                                        <txtProductVersion>${project.version}</txtProductVersion>
                                        <fileDescription>${project.description}</fileDescription>
                                        <productName>${app.name}</productName>
                                        <companyName>Glyco-PAINT</companyName>
                                    </versionInfo>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <!-- =============================================================== -->
    <!-- External repositories -->
    <!-- =============================================================== -->
    <repositories>
        <repository>
            <id>scijava.public</id>
            <url>https://maven.scijava.org/content/groups/public</url>
        </repository>
        <repository>
            <id>ome.releases</id>
            <url>https://artifacts.openmicroscopy.org/artifactory/maven/</url>
        </repository>
    </repositories>

</project>